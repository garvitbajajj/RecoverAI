node_modules/

# Secrets. Never commit API keys -- including Razorpay test-mode keys.
.env
.env.*
!.env.example

# Local project context, not part of the submission
CLAUDE.md

# Regenerated every run — `npm start` then `npm run dashboard`
data/audit_log.jsonl
data/run_report.json
dashboard.html

npm-debug.log*
.DS_Store
